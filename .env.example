# 部署到 Cloudflare Pages 时，前端请求的 API 地址（构建时注入 Vite）。
VITE_API_BASE=https://api.lemonhub.net

# Worker 环境变量（wrangler.jsonc vars，此处仅为文档）。
LEMONMAIL_DOMAIN=lemonhub.net
MAIL_FORWARD_PROVIDER=cloudflare-email-routing
MAIL_FORWARD_WEBHOOK_SECRET=replace-me
